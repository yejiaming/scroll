/**
 *
 * @authors liwb (you@example.org)
 * @date    2017/2/9 18:58
 * @version $ 不需要每次根据不同环境，手动修改的全局变量，下面只是事例，可以根据具体项目所需进行修改
 */

/* name module */

module.exports = {
  // openapi
  OPEN_API: 'vipstu/v1/',
  // 灰度升级
  UPGRADE_API: 'upgrade/',
  // 流式服务，如上传图片路径
  UPLOADING_API: 'fileOpenapi/'
};
